#!/bin/bash

# UICTL - Unified GNOME Configuration Control
# Jednotné rozhraní pro backup a restore GNOME nastavení

# Barevné výstupy
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Globální proměnné
FORCE=false
DRY_RUN=false
QUIET=false
VERBOSE=false

# Funkce pro barevné výpisy
print_info() { [ "$QUIET" != "true" ] && echo -e "${BLUE}ℹ️  $1${NC}"; }
print_success() { [ "$QUIET" != "true" ] && echo -e "${GREEN}✅ $1${NC}"; }
print_warning() { [ "$QUIET" != "true" ] && echo -e "${YELLOW}⚠️  $1${NC}"; }
print_error() { echo -e "${RED}❌ $1${NC}" >&2; }
print_verbose() { [ "$VERBOSE" = "true" ] && echo -e "${BLUE}🔍 $1${NC}"; }

# Získání aktuálního adresáře scriptu
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKUP_DIR="$SCRIPT_DIR/backups"

# Hlavní help funkce
show_main_help() {
    cat << 'EOF'
🖥️  UICTL - Unified GNOME Configuration Control

Použití: uictl <příkaz> [možnosti] [argumenty]

Příkazy:
  backup              Vytvoří zálohu GNOME nastavení
  restore <backup>    Obnoví zálohu GNOME nastavení  
  list                Zobrazí dostupné zálohy
  status              Zobrazí info o nejnovější záloze
  clean [počet]       Vyčistí staré zálohy (ponechá posledních N)
  help [příkaz]       Zobrazí nápovědu

Globální možnosti:
  -f, --force         Neptat se na potvrzení
  -d, --dry-run       Pouze zobrazí co by se dělalo (restore/clean)
  -q, --quiet         Tichý režim
  -v, --verbose       Podrobný výstup
  -h, --help          Zobrazí nápovědu

Příklady:
  uictl backup                    Vytvoří zálohu
  uictl list                      Zobrazí zálohy
  uictl restore latest            Obnoví nejnovější zálohu
  uictl restore backup_20240630   Obnoví specifickou zálohu
  uictl status                    Ukáže info o nejnovější záloze
  uictl clean 5                   Ponechá jen posledních 5 záloh
  uictl help restore              Nápověda pro příkaz restore

Pro podrobnou nápovědu k příkazu: uictl help <příkaz>
EOF
}

# Help pro jednotlivé příkazy
show_command_help() {
    case "$1" in
        backup)
            cat << 'EOF'
📦 uictl backup - Vytvoří zálohu GNOME nastavení

Použití: uictl backup [možnosti]

Vytvoří kompletní zálohu všech GNOME nastavení včetně:
- Dconf nastavení (všechna GNOME nastavení)
- Konfigurační soubory (~/.config)
- GNOME Shell extensions
- GTK témata a nastavení
- Monitor konfigurace
- Autostart aplikace

Možnosti:
  -q, --quiet    Tichý režim (pouze chyby)
  -v, --verbose  Podrobný výstup

Příklady:
  uictl backup          Standardní záloha
  uictl backup -v       Podrobná záloha
EOF
            ;;
        restore)
            cat << 'EOF'
🔄 uictl restore - Obnoví zálohu GNOME nastavení

Použití: uictl restore <backup> [možnosti]

Argumenty:
  backup              Název zálohy (backup_YYYYMMDD_HHMMSS)
                     nebo 'latest' pro nejnovější

Možnosti:
  -f, --force        Neptat se na potvrzení
  -d, --dry-run      Pouze ukáže co by se dělalo
  -q, --quiet        Tichý režim
  -v, --verbose      Podrobný výstup

Příklady:
  uictl restore latest              Obnoví nejnovější zálohu
  uictl restore backup_20240630     Obnoví specifickou zálohu  
  uictl restore -d latest           Dry-run režim
  uictl restore -f latest           Bez potvrzení

⚠️  POZOR: Restore přepíše vaše současné GNOME nastavení!
Před obnovením se automaticky vytvoří záloha současného stavu.
EOF
            ;;
        list)
            cat << 'EOF'
📂 uictl list - Zobrazí dostupné zálohy

Použití: uictl list [možnosti]

Zobrazí seznam všech dostupných záloh včetně:
- Název zálohy
- Datum a čas vytvoření
- Velikost zálohy
- Symlink na nejnovější zálohu

Možnosti:
  -q, --quiet    Pouze názvy záloh
  -v, --verbose  Podrobné informace

Příklad:
  uictl list       Zobrazí všechny zálohy
EOF
            ;;
        status)
            cat << 'EOF'
📊 uictl status - Zobrazí informace o nejnovější záloze

Použití: uictl status [možnosti]

Zobrazí podrobné informace o nejnovější záloze:
- Datum a čas vytvoření
- Velikost zálohy
- Systémové informace z doby vytvoření
- Obsah zálohy

Příklad:
  uictl status     Zobrazí info o nejnovější záloze
EOF
            ;;
        clean)
            cat << 'EOF'
🧹 uictl clean - Vyčistí staré zálohy

Použití: uictl clean [počet] [možnosti]

Argumenty:
  počet               Počet nejnovějších záloh k ponechání (výchozí: 10)

Možnosti:
  -f, --force        Neptat se na potvrzení
  -d, --dry-run      Pouze ukáže co by se smazalo
  -q, --quiet        Tichý režim
  -v, --verbose      Podrobný výstup

Příklady:
  uictl clean              Ponechá posledních 10 záloh
  uictl clean 5            Ponechá posledních 5 záloh
  uictl clean -d           Ukáže co by se smazalo
EOF
            ;;
        *)
            print_error "Neznámý příkaz: $1"
            echo "Dostupné příkazy: backup, restore, list, status, clean"
            ;;
    esac
}

# Funkce pro výpis dostupných záloh
list_backups() {
    print_verbose "Kontroluji adresář záloh: $BACKUP_DIR"
    
    if [ ! -d "$BACKUP_DIR" ]; then
        print_warning "Adresář se zálohami neexistuje: $BACKUP_DIR"
        return 1
    fi
    
    local backup_files=($(ls "$BACKUP_DIR"/backup_* 2>/dev/null || true))
    if [ ${#backup_files[@]} -eq 0 ]; then
        print_warning "Žádné zálohy nenalezeny"
        return 1
    fi
    
    if [ "$QUIET" != "true" ]; then
        print_info "📂 Dostupné zálohy v $BACKUP_DIR:"
        echo
    fi
    
    for backup in "$BACKUP_DIR"/backup_*; do
        if [ -d "$backup" ]; then
            backup_name=$(basename "$backup")
            backup_date=$(echo "$backup_name" | sed 's/backup_//' | sed 's/_/ /')
            backup_size=$(du -sh "$backup" 2>/dev/null | cut -f1 || echo "N/A")
            
            if [ "$QUIET" = "true" ]; then
                echo "$backup_name"
            else
                printf "  %-25s %s (%s)\n" "$backup_name" "$backup_date" "$backup_size"
            fi
        fi
    done
    
    if [ "$QUIET" != "true" ]; then
        echo
        if [ -L "$BACKUP_DIR/latest" ]; then
            latest_target=$(readlink "$BACKUP_DIR/latest")
            print_info "🔗 Nejnovější záloha: $latest_target"
        fi
    fi
}

# Funkce pro zobrazení stavu
show_status() {
    if [ ! -L "$BACKUP_DIR/latest" ]; then
        print_warning "Žádná záloha nenalezena"
        return 1
    fi
    
    latest_backup="$BACKUP_DIR/$(readlink "$BACKUP_DIR/latest")"
    
    if [ ! -d "$latest_backup" ]; then
        print_error "Nejnovější záloha neexistuje: $latest_backup"
        return 1
    fi
    
    print_info "📊 Status nejnovější zálohy:"
    echo
    
    backup_name=$(basename "$latest_backup")
    backup_date=$(echo "$backup_name" | sed 's/backup_//' | sed 's/_/ /')
    backup_size=$(du -sh "$latest_backup" 2>/dev/null | cut -f1 || echo "N/A")
    
    printf "  Název:      %s\n" "$backup_name"
    printf "  Datum:      %s\n" "$backup_date"
    printf "  Velikost:   %s\n" "$backup_size"
    echo
    
    if [ -f "$latest_backup/backup-info.txt" ]; then
        print_info "📋 Informace o záloze:"
        cat "$latest_backup/backup-info.txt" | sed 's/^/  /'
        echo
    fi
    
    if [ "$VERBOSE" = "true" ]; then
        print_info "📁 Obsah zálohy:"
        find "$latest_backup" -type f | sed 's|^'"$latest_backup"'/|  |' | sort
    fi
}

# Funkce pro čištění starých záloh
clean_old_backups() {
    local keep_count=${1:-10}
    
    print_verbose "Hledám zálohy k vyčištění, ponechám posledních $keep_count"
    
    if [ ! -d "$BACKUP_DIR" ]; then
        print_warning "Adresář se zálohami neexistuje"
        return 0
    fi
    
    # Najít všechny zálohy seřazené podle data (nejnovější první)
    local backups=()
    while IFS= read -r -d '' backup; do
        if [ -d "$backup" ]; then
            backups+=("$backup")
        fi
    done < <(find "$BACKUP_DIR" -maxdepth 1 -name "backup_*" -type d -print0 | sort -z -r)
    local total_backups=${#backups[@]}
    
    if [ $total_backups -le $keep_count ]; then
        print_info "Nalezeno $total_backups záloh, žádné čištění není potřeba (limit: $keep_count)"
        return 0
    fi
    
    local to_delete=(${backups[@]:$keep_count})
    local delete_count=${#to_delete[@]}
    
    print_info "Nalezeno $total_backups záloh, bude smazáno $delete_count starých záloh"
    
    if [ "$DRY_RUN" = "true" ]; then
        print_warning "🧪 DRY-RUN režim - zálohy by byly smazány:"
        for backup in "${to_delete[@]}"; do
            backup_name=$(basename "$backup")
            backup_size=$(du -sh "$backup" 2>/dev/null | cut -f1 || echo "N/A")
            echo "  $backup_name ($backup_size)"
        done
        return 0
    fi
    
    if [ "$FORCE" != "true" ]; then
        echo
        print_warning "🗑️  Budou smazány následující zálohy:"
        for backup in "${to_delete[@]}"; do
            backup_name=$(basename "$backup")
            backup_size=$(du -sh "$backup" 2>/dev/null | cut -f1 || echo "N/A")
            echo "  $backup_name ($backup_size)"
        done
        echo
        read -p "Pokračovat se smazáním? (ano/ne): " -r
        if [[ ! $REPLY =~ ^[Aa]no$ ]]; then
            print_info "Operace zrušena"
            return 0
        fi
    fi
    
    # Smazání starých záloh
    for backup in "${to_delete[@]}"; do
        if [ -d "$backup" ]; then
            backup_name=$(basename "$backup")
            print_verbose "Mazám zálohu: $backup_name"
            rm -rf "$backup"
            print_success "Smazáno: $backup_name"
        fi
    done
    
    print_success "Vyčištění dokončeno - smazáno $delete_count záloh"
}

# Funkce pro vytvoření zálohy (ze starého backup.sh)
create_backup() {
    local timestamp=$(date +%Y%m%d_%H%M%S)
    local current_backup="$BACKUP_DIR/backup_$timestamp"
    
    print_info "🔄 Spouštím zálohu GNOME nastavení..."
    print_verbose "📂 Cílový adresář: $current_backup"
    
    mkdir -p "$current_backup"
    mkdir -p "$current_backup/config"
    mkdir -p "$current_backup/local"
    mkdir -p "$current_backup/dconf"
    
    print_info "📊 Exportuji dconf nastavení..."
    dconf dump / > "$current_backup/dconf/all-settings.ini"
    dconf dump /org/gnome/shell/extensions/ > "$current_backup/dconf/extensions-settings.ini"
    dconf dump /org/gnome/desktop/ > "$current_backup/dconf/desktop-settings.ini"
    dconf dump /org/gnome/settings-daemon/ > "$current_backup/dconf/settings-daemon.ini"
    dconf dump /org/gnome/mutter/ > "$current_backup/dconf/mutter-settings.ini"
    print_success "Dconf nastavení exportována"
    
    print_info "📁 Kopíruji konfigurační soubory..."
    [ -d ~/.config/dconf ] && cp -r ~/.config/dconf "$current_backup/config/" && print_verbose "dconf složka zkopírována"
    [ -d ~/.config/gtk-2.0 ] && cp -r ~/.config/gtk-2.0 "$current_backup/config/" && print_verbose "GTK 2.0 nastavení zkopírována"
    [ -d ~/.config/gtk-3.0 ] && cp -r ~/.config/gtk-3.0 "$current_backup/config/" && print_verbose "GTK 3.0 nastavení zkopírována"
    [ -d ~/.config/gtk-4.0 ] && cp -r ~/.config/gtk-4.0 "$current_backup/config/" && print_verbose "GTK 4.0 nastavení zkopírována"
    [ -d ~/.config/autostart ] && cp -r ~/.config/autostart "$current_backup/config/" && print_verbose "Autostart aplikace zkopírovány"
    [ -d ~/.config/tiling-assistant ] && cp -r ~/.config/tiling-assistant "$current_backup/config/" && print_verbose "Tiling Assistant nastavení zkopírána"
    [ -d ~/.config/gnome-session ] && cp -r ~/.config/gnome-session "$current_backup/config/" && print_verbose "GNOME Session nastavení zkopírána"
    
    [ -f ~/.config/monitors.xml ] && cp ~/.config/monitors.xml "$current_backup/config/" && print_verbose "Monitor nastavení zkopírováno"
    [ -f ~/.config/mimeapps.list ] && cp ~/.config/mimeapps.list "$current_backup/config/" && print_verbose "MIME aplikace zkopírovány"
    [ -f ~/.config/user-dirs.dirs ] && cp ~/.config/user-dirs.dirs "$current_backup/config/" && print_verbose "Uživatelské adresáře zkopírovány"
    print_success "Konfigurační soubory zkopírovány"
    
    print_info "🔌 Zálohuju GNOME Shell extensions..."
    if [ -d ~/.local/share/gnome-shell/extensions ]; then
        cp -r ~/.local/share/gnome-shell/extensions "$current_backup/local/"
        print_success "Uživatelské extensions zkopírovány"
    else
        print_warning "Žádné uživatelské extensions nenalezeny"
    fi
    
    [ -d ~/.local/share/gnome-shell ] && cp -r ~/.local/share/gnome-shell "$current_backup/local/" && print_verbose "GNOME Shell data zkopírována"
    [ -d ~/.local/share/applications ] && cp -r ~/.local/share/applications "$current_backup/local/" && print_verbose "Uživatelské aplikace zkopírovány"
    
    print_info "📝 Ukládám metadata..."
    gsettings get org.gnome.shell enabled-extensions > "$current_backup/enabled-extensions.txt"
    ls /usr/share/gnome-shell/extensions/ > "$current_backup/system-extensions.txt" 2>/dev/null || echo "Žádné systémové extensions" > "$current_backup/system-extensions.txt"
    
    echo "GNOME Backup - $(date)" > "$current_backup/backup-info.txt"
    echo "Hostname: $(hostname)" >> "$current_backup/backup-info.txt"
    echo "User: $(whoami)" >> "$current_backup/backup-info.txt"
    echo "GNOME Version: $(gnome-shell --version 2>/dev/null || echo 'Unknown')" >> "$current_backup/backup-info.txt"
    echo "OS: $(lsb_release -d 2>/dev/null | cut -d: -f2 | xargs || echo 'Unknown')" >> "$current_backup/backup-info.txt"
    echo "Kernel: $(uname -r)" >> "$current_backup/backup-info.txt"
    print_success "Metadata uložena"
    
    cd "$BACKUP_DIR" && ln -sf "backup_$timestamp" latest
    
    print_info "🧹 Čistím dočasné soubory ze zálohy..."
    find "$current_backup" -name "*.backup-[0-9]*" -type d -exec rm -rf {} + 2>/dev/null || true
    find "$current_backup" -name "*.backup-[0-9]*" -type f -exec rm -f {} + 2>/dev/null || true
    print_verbose "Dočasné soubory vyčištěny"
    
    local backup_size=$(du -sh "$current_backup" | cut -f1)
    
    print_success "🎉 Záloha úspěšně dokončena!"
    print_info "📂 Umístění zálohy: $current_backup"
    print_info "🔗 Symlink na nejnovější: $BACKUP_DIR/latest"
    print_info "💾 Velikost zálohy: $backup_size"
    
    if [ "$VERBOSE" = "true" ]; then
        echo
        print_info "📋 Obsah zálohy:"
        find "$current_backup" -type f | sed 's|^'"$current_backup"'/|  |' | sort
    fi
}

# Funkce pro vytvoření zálohy před obnovením
create_pre_restore_backup() {
    print_info "💾 Vytvářím zálohu před obnovením..."
    local pre_restore_backup="$BACKUP_DIR/pre-restore-backup_$(date +%Y%m%d_%H%M%S)"
    
    mkdir -p "$pre_restore_backup/config"
    mkdir -p "$pre_restore_backup/dconf"
    
    dconf dump / > "$pre_restore_backup/dconf/current-settings.ini" 2>/dev/null || true
    [ -d ~/.config/dconf ] && cp -r ~/.config/dconf "$pre_restore_backup/config/" 2>/dev/null || true
    [ -d ~/.local/share/gnome-shell/extensions ] && cp -r ~/.local/share/gnome-shell/extensions "$pre_restore_backup/" 2>/dev/null || true
    
    print_success "Záloha před obnovením vytvořena: $(basename "$pre_restore_backup")"
}

# Funkce pro obnovení nastavení (ze starého restore.sh)
restore_settings() {
    local restore_path="$1"
    
    if [ ! -d "$restore_path" ]; then
        print_error "Záloha neexistuje: $restore_path"
        return 1
    fi
    
    print_info "🔄 Obnovuji GNOME nastavení ze zálohy: $(basename "$restore_path")"
    
    if [ "$DRY_RUN" = "true" ]; then
        print_warning "🧪 DRY-RUN režim - pouze zobrazuji co by se dělalo"
    fi
    
    if [ -f "$restore_path/backup-info.txt" ] && [ "$VERBOSE" = "true" ]; then
        print_info "📋 Informace o záloze:"
        cat "$restore_path/backup-info.txt" | sed 's/^/  /'
        echo
    fi
    
    # Obnovení dconf nastavení
    if [ -f "$restore_path/dconf/all-settings.ini" ]; then
        print_info "📊 Obnovuji dconf nastavení..."
        if [ "$DRY_RUN" != "true" ]; then
            dconf load / < "$restore_path/dconf/all-settings.ini"
            print_success "Dconf nastavení obnovena"
        else
            print_verbose "  Obnovil bych: $restore_path/dconf/all-settings.ini"
        fi
    else
        print_warning "Dconf záloha nenalezena"
    fi
    
    # Obnovení konfiguračních souborů
    if [ -d "$restore_path/config" ]; then
        print_info "📁 Obnovuji konfigurační soubory..."
        for config_item in "$restore_path/config"/*; do
            if [ -e "$config_item" ]; then
                local item_name=$(basename "$config_item")
                local target_path="$HOME/.config/$item_name"
                
                if [ "$DRY_RUN" != "true" ]; then
                    if [ -e "$target_path" ]; then
                        mv "$target_path" "/tmp/$(basename "$target_path").backup-$(date +%s)" 2>/dev/null || true
                    fi
                    cp -r "$config_item" "$target_path"
                    print_verbose "Obnoveno: $item_name"
                else
                    print_verbose "  Obnovil bych: $item_name -> $target_path"
                fi
            fi
        done
        [ "$DRY_RUN" != "true" ] && print_success "Konfigurační soubory obnoveny"
    fi
    
    # Obnovení extensions
    if [ -d "$restore_path/local/extensions" ]; then
        print_info "🔌 Obnovuji GNOME Shell extensions..."
        if [ "$DRY_RUN" != "true" ]; then
            mkdir -p "$HOME/.local/share/gnome-shell"
            if [ -d "$HOME/.local/share/gnome-shell/extensions" ]; then
                mv "$HOME/.local/share/gnome-shell/extensions" "/tmp/extensions.backup-$(date +%s)" 2>/dev/null || true
            fi
            cp -r "$restore_path/local/extensions" "$HOME/.local/share/gnome-shell/extensions"
            print_success "Extensions obnoveny"
        else
            print_verbose "  Obnovil bych uživatelské extensions"
        fi
    fi
    
    # Obnovení seznamu aktivních extensions
    if [ -f "$restore_path/enabled-extensions.txt" ]; then
        print_info "✅ Obnovuji seznam aktivních extensions..."
        if [ "$DRY_RUN" != "true" ]; then
            local enabled_extensions=$(cat "$restore_path/enabled-extensions.txt")
            gsettings set org.gnome.shell enabled-extensions "$enabled_extensions"
            print_success "Seznam aktivních extensions obnoven"
        else
            print_verbose "  Obnovil bych: $(cat "$restore_path/enabled-extensions.txt")"
        fi
    fi
    
    if [ "$DRY_RUN" != "true" ]; then
        echo
        print_success "🎉 Obnovení dokončeno!"
        print_warning "⚠️  Doporučuje se restartovat GNOME Shell (Alt+F2, pak napište 'r')"
        print_info "💡 Nebo se odhlaste a znovu přihlaste"
    else
        echo
        print_info "🧪 DRY-RUN dokončen - žádné změny nebyly provedeny"
    fi
}


# Hlavní logika
main() {
    # Parsování globálních argumentů - přímo v main kvůli proměnným
    local remaining_args=()
    
    while [[ $# -gt 0 ]]; do
        case $1 in
            -f|--force)
                FORCE=true
                shift
                ;;
            -d|--dry-run)
                DRY_RUN=true
                shift
                ;;
            -q|--quiet)
                QUIET=true
                shift
                ;;
            -v|--verbose)
                VERBOSE=true
                shift
                ;;
            -h|--help)
                if [ -n "$2" ] && [[ ! "$2" =~ ^- ]]; then
                    show_command_help "$2"
                    exit 0
                else
                    show_main_help
                    exit 0
                fi
                ;;
            *)
                # Přidat nerozpoznaný argument do výsledku
                remaining_args+=("$1")
                shift
                ;;
        esac
    done
    
    if [ ${#remaining_args[@]} -eq 0 ]; then
        show_main_help
        exit 0
    fi
    
    local command="${remaining_args[0]}"
    local cmd_args=("${remaining_args[@]:1}")
    
    case "$command" in
        backup)
            create_backup
            ;;
        restore)
            if [ ${#cmd_args[@]} -eq 0 ]; then
                print_error "Chybí název zálohy"
                echo "Použijte: uictl restore <backup>"
                echo "Nebo: uictl list pro zobrazení dostupných záloh"
                exit 1
            fi
            
            local backup_name="${cmd_args[0]}"
            local restore_path
            
            if [[ "$backup_name" == "latest" ]]; then
                if [ -L "$BACKUP_DIR/latest" ]; then
                    restore_path="$BACKUP_DIR/$(readlink "$BACKUP_DIR/latest")"
                    backup_name="latest ($(readlink "$BACKUP_DIR/latest"))"
                else
                    print_error "'latest' symlink neexistuje"
                    print_info "Použijte 'uictl list' pro zobrazení dostupných záloh"
                    exit 1
                fi
            elif [[ "$backup_name" == backup_* ]]; then
                restore_path="$BACKUP_DIR/$backup_name"
            else
                restore_path="$BACKUP_DIR/backup_$backup_name"
            fi
            
            if [ ! -d "$restore_path" ]; then
                print_error "Záloha neexistuje: $restore_path"
                list_backups
                exit 1
            fi
            
            # Potvrzení (pokud není force mode nebo dry-run)
            if [ "$FORCE" != "true" ] && [ "$DRY_RUN" != "true" ]; then
                echo
                print_warning "🚨 POZOR: Tato operace přepíše vaše současné GNOME nastavení!"
                print_info "Záloha k obnovení: $backup_name"
                print_info "Před obnovením bude vytvořena záloha současného stavu."
                echo
                read -p "Pokračovat? (ano/ne): " -r
                if [[ ! $REPLY =~ ^[Aa]no$ ]]; then
                    print_info "Operace zrušena"
                    exit 0
                fi
            fi
            
            # Vytvoření zálohy před obnovením (pokud není dry-run)
            if [ "$DRY_RUN" != "true" ]; then
                create_pre_restore_backup
            fi
            
            restore_settings "$restore_path"
            ;;
        list)
            list_backups
            ;;
        status)
            show_status
            ;;
        clean)
            # Parsování argumentů pro clean - první non-option argument je počet
            local keep_count=10
            local i=0
            while [ $i -lt ${#cmd_args[@]} ]; do
                arg="${cmd_args[$i]}"
                if [[ "$arg" =~ ^[0-9]+$ ]]; then
                    keep_count="$arg"
                    break
                fi
                ((i++))
            done
            clean_old_backups "$keep_count"
            ;;
        help)
            if [ ${#cmd_args[@]} -gt 0 ]; then
                show_command_help "${cmd_args[0]}"
            else
                show_main_help
            fi
            ;;
        *)
            print_error "Neznámý příkaz: $command"
            echo "Použijte 'uictl help' pro zobrazení dostupných příkazů"
            exit 1
            ;;
    esac
}

# Spuštění hlavní funkce
main "$@"